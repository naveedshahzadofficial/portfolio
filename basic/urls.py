from django.urls import path
from basic import views

app_name = 'basic'

urlpatterns = [
    # path('', views.index, name='topics.index'),
    # path('topics/create/', views.create, name='topics.create'),
    # path('topics/store/', views.store, name='topics.store'),
]
